locustfile = shaped_locustfile.py
headless = false
; master = false
expect-workers = 4
host = http://localhost:8080/
html = results.html
; users = 100
; spawn-rate = 10
; run-time = 10m
; tags = [Critical, Normal]
; tasks = [GetTasks, DeleteTasks, PostTasks, PutTasks]