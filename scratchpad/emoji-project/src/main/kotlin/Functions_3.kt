package me.marco.function3

private typealias `📜` = String
private typealias `📦` = List<*>


fun main() {
    val `📦` = listOf("a", "b", "c")


    `📦`.forEach { `🔈🌍`(it) }
}


fun `🔈🌍`(`✉️`: `📜`) {
    println(`✉️`)
}
